apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: post-backstage-template4
  description: post-backstage-template
  annotations:
    tags: "infra, aws, lambda"
spec:
  type: infra
  lifecycle: experimental
  owner: rca0
